<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>net.ladstatt</groupId>
        <artifactId>apps</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <groupId>net.ladstatt.apps.sudoku</groupId>
    <artifactId>sudoku-javafx</artifactId>
    <name>net.ladstatt.apps.sudoku.sudoku-javafx</name>

    <packaging>jar</packaging>

    <dependencies>
        <!-- you have to install opencv in your local repository -->
        <!-- for this project, i named the opencv.jar to be a org.opencv opencv-java7 artifact -->

        <dependency>
            <groupId>net.ladstatt.apps.sudoku</groupId>
            <artifactId>sudoku-core</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.jfxtras</groupId>
            <artifactId>jfxtras-controls</artifactId>
            <version>8.0-r4-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.jfxtras</groupId>
            <artifactId>jfxtras-labs</artifactId>
            <version>8.0-r4-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
        </dependency>
        <!-- you have to install opencv in your local repository -->
        <!-- for this project, i named the opencv.jar to be a org.opencv opencv-java7 artifact -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scalacheck</groupId>
            <artifactId>scalacheck_2.11</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scalatest</groupId>
            <artifactId>scalatest_2.11</artifactId>
        </dependency>
        <dependency>
            <groupId>io.reactivex</groupId>
            <artifactId>rxjava</artifactId>
            <version>1.0.1</version>
        </dependency>
        <dependency>
            <groupId>io.reactivex</groupId>
            <artifactId>rxscala_2.11</artifactId>
            <version>0.23.1</version>
        </dependency>
    </dependencies>
    <build>
        <!-- <sourceDirectory>src/main/scala</sourceDirectory> -->
        <plugins>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>com.zenjava</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>create-jfx-jar</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <vendor>${project.organization.name}</vendor>
                    <mainClass>net.ladstatt.apps.sudoku.fx.SudokuFX</mainClass>
                </configuration>
            </plugin>

        </plugins>

    </build>


</project>


